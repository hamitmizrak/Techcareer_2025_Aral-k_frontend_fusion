    <!-- start nav -->
    <nav class="navbar navbar-expand-md navbar-dark bg-dark fixed-top mb-5">
      <div class="container">
        <a class="navbar-brand" href="/">Frontend Fusion</a>

        <!-- Toggler button -->
        <button
          class="navbar-toggler d-md-none"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#collapsibleNavId"
          aria-controls="collapsibleNavId"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>

        <!-- navbar menü -->
        <div id="collapsibleNavId" class="collapse navbar-collapse">
          <!-- LEFT -->
          <ul class="navbar_left_class navbar-nav">
            <li class="nav-item">
              <a class="nav-link" href="#">Hakkımızda</a>
            </li>
            <li class="nav-item dropdown">
              <a
                class="nav-link dropdown-toggle"
                href="#"
                id="dropdownId"
                data-bs-toggle="dropdown"
                aria-haspopup="true"
                aria-expanded="false"
              >
                Blog
              </a>
              <div class="dropdown-menu" aria-labelledby="dropdownId">
                <a class="dropdown-item" href="#">Action 1</a>
                <a class="dropdown-item" href="#">Action 2</a>
              </div>
            </li>
            <li class="nav-item dropdown">
              <a class="nav-link" href="#">İletişim</a>
            </li>
          </ul>

          <!-- CENTER -->
          <form class="d-flex navbar_center_class">
            <input class="form-control me-2" type="text" placeholder="Arama ... " />
            <button class="btn btn-outline-info my-2 my-sm-0" type="submit">Ara</button>
          </form>

          <!-- RIGHT -->
          <!-- LOGIN REGISTER DASHBOARD -->
          <ul class="navbar-nav navbar_right_class" id="navRightArea">
            <!-- Bu blok kullanıcı login değilse gösterilecek menü (public) -->
            <% if (!currentUserEmail) {%>

              <!-- Public Menu -->
            <li class="nav-item">
              <a class="nav-link" href="/register" aria-current="page">Kayıt Ol</a>
            </li>

            <li class="nav-item">
              <a class="nav-link" href="/login" aria-current="page">Giriş Yap</a>
            </li>
            <% } else { %>
              <!-- Session'a göre backend tarflı menümüz(Login olmuşsa gelecek) -->
            <!-- Bu blok kullanıcı login ise gösterilecek menü (private) -->
            <li class="nav-item">
              <a class="nav-link btn btn-primary" href="/admin_page" aria-current="page">Admin Paneli</a>
            </li>

            <li class="nav-item">
              <a class="nav-link btn btn-danger" href="/logout" aria-current="page">Çıkış Yap</a>
            </li>
            <% } %>
          </ul>
        </div>
      </div>
    </nav>
    <!-- end nav -->
